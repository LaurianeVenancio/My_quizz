{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ question.question }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('question_index') }}" class="btn btn-danger">back to list</a>

    <a href="{{ path('question_edit', {'id': question.id}) }}" class="btn btn-primary">edit</a>

    {{ include('question/_delete_form.html.twig') }}
{% endblock %}
